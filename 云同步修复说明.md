# 云同步修复说明

## 问题描述
Gitee API 的文件上传接口需要区分：
- **创建新文件**：使用 POST 方法，不需要 SHA
- **更新现有文件**：使用 PUT 方法，需要 SHA

原代码错误地将所有请求都使用 PUT 方法，导致创建新文件时失败（错误：sha is missing）

## 修复内容

### 1. 修复同步到云端 (`syncToCloud`)

```javascript
// 先获取文件的SHA（如果文件存在）
const getResponse = await fetch(...);
let sha = null;
if (getResponse.ok) {
    const fileInfo = await getResponse.json();
    // 处理数组或对象的情况
    if (Array.isArray(fileInfo)) {
        if (fileInfo.length > 0 && fileInfo[0].sha) {
            sha = fileInfo[0].sha;
        }
    } else if (fileInfo.sha) {
        sha = fileInfo.sha;
    }
}

// Gitee API: 创建新文件用 POST，更新文件用 PUT
const method = sha ? 'PUT' : 'POST';
const body = {
    content: btoa(unescape(encodeURIComponent(content))),
    message: message,
    branch: this.branch
};
if (sha) {
    body.sha = sha;
}
```

### 2. 修复从云端同步 (`syncFromCloud`)

```javascript
// 处理数组或对象的情况
let contentStr = null;
if (Array.isArray(fileInfo)) {
    if (fileInfo.length > 0 && fileInfo[0].content) {
        contentStr = decodeURIComponent(escape(atob(fileInfo[0].content)));
    }
} else if (fileInfo.content) {
    contentStr = decodeURIComponent(escape(atob(fileInfo.content)));
}
```

### 3. 修复创建初始文件 (`createInitialFile`)

```javascript
// 使用 POST 创建新文件
await fetch(..., {
    method: 'POST',  // 不是 PUT
    body: JSON.stringify({
        content: this.getDataString(),
        message: '初始化任务本数据',
        branch: this.branch  // 需要指定分支
    })
});
```

## 测试结果

测试工具显示：
- ✅ 验证 Token 成功
- ✅ 检查仓库成功
- ✅ 检查 tasks.json 文件成功（不存在）
- ✅ 创建文件成功（使用 POST 方法）
- 🎉 同步功能已打通

## 使用说明

1. **在电脑上**：
   - 打开 `index.html`
   - 登录 Gitee 云同步
   - 添加任务后，点击"立即同步"

2. **在手机上**：
   - 打开 https://kuangquanshui73-create.github.io/tasks
   - 登录 Gitee 云同步
   - 点击"立即同步"

数据就会在两端自动同步！

## 文件清单

已修复文件：
- `D:\工作安排管理系统\app.js` - 主程序
- `D:\工作安排管理系统\test-gitee-api.html` - 测试工具
